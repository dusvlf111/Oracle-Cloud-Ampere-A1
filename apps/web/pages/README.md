// Intentionally empty: this root-level Pages Router directory exists only to
// stop Next from falling back to the FSD `src/pages` layer (which is NOT a
// router) during `next build`. All real routes live in the App Router (`app/`).
